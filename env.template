# Prediction Odds Research Agent - Environment Template
#
# Copy this file to `.env` (DO NOT commit `.env`).
# Windows PowerShell:
#   Copy-Item env.template .env
#
# Then edit `.env` with your real credentials.

# Switch mock -> live
# Set to false to use live APIs (public endpoints work without API keys!)
MOCK_MODE=false

# Database (defaults shown)
DATABASE_URL_ASYNC=postgresql+asyncpg://pm:pm@localhost:5432/pm_research
DATABASE_URL_SYNC=postgresql+psycopg://pm:pm@localhost:5432/pm_research

# Kalshi API credentials (OPTIONAL - public market data endpoints work without auth)
# Auth only needed for private endpoints (trading, account info)
# Public endpoints: https://api.cx.kalshi.com/trade-api/v2/markets
KALSHI_API_KEY=
KALSHI_API_SECRET=

# Polymarket API key (OPTIONAL - CLOB GraphQL endpoints work without auth)
# Public endpoints: https://clob.polymarket.com/graphql
# Auth only needed for private endpoints (trading, account info)
POLYMARKET_API_KEY=

# Optional: alerts
ALERT_EMAIL=
ALERT_SLACK_WEBHOOK=

# Optional: debug logging
DEBUG=false


